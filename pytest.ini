; Root config so `pytest` run from the repo root finds the backend suite
; and never recurses into the venv or node_modules.
[pytest]
testpaths = backend/tests
norecursedirs = backend/venv frontend/node_modules .git
